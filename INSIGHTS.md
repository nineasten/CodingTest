# 인사이트 — 시험 직전 최종 병기

`/retro`가 문제를 끝낼 때마다 해당 유형 항목을 갱신한다(새 내용이면 추가, 이미 있으면
더 정확하게 다듬음). **시험 직전엔 이 파일만 읽으면 감이 복구되도록** 유지한다.

각 항목 형식:
```
## 유형명
- 신호: 문제에 이런 말이 나오면 이 유형
- 골격: 표준 구현 스니펫 (또는 lib/playbook/의 몇 번 노트 참고)
- 함정: 자주 틀리는 지점
- 내가 틀린 이력: 문제명(원인) 나열
```

아직 문제를 풀지 않아 항목이 없다. `/next`로 첫 문제부터 시작.

---

## 구현·시뮬레이션
- 신호: "빠진 숫자/원소를 찾아라", "0~N 중 일부만 무작위로 주어진다"
- 골격: 빠진 원소가 **정확히 1개**면 `합공식(등차수열) - sum(arr)`가 가장 가볍다
  (예: 0~9 합은 45 → `45 - sum(numbers)`). 여러 개거나 범위가 안 맞으면
  `set(range(n)) - set(arr)` 차집합.
- **상태가 여러 번 갱신되고 최종값만 출력에 반영해야 할 때**: 기록을 2회 순회.
  1회차는 전체를 훑어 "최종 상태"만 딕셔너리로 수집(중간 갱신 기록도 여기 포함),
  2회차는 원본 순서대로 "출력에 실제로 나타나는 이벤트"만 골라 최종 상태로 렌더링.
  상태만 바꾸고 출력엔 안 나오는 이벤트(예: 닉네임 변경)를 헷갈리지 말 것.
- 함정: 반복문 + 조건문으로 직접 필터링하려 하면 로직이 꼬이기 쉬움 — 먼저
  "집합 연산/산술식으로 한 줄에 표현 가능한가"부터 검토
- 관련 문제: 없는 숫자 더하기(86051), 오픈채팅방(42888)

## 2D 격자
- 신호: "N×M 지도", "상하좌우 이동", "좌표평면 위 도형", "테두리를 따라 이동"
- 골격: 방향벡터 `dr=[-1,1,0,0]`, `dc=[0,0,-1,1]` + `for k in range(4)` + 범위체크
  `0 <= nr < N and 0 <= nc < M`. 2D 배열은 반드시 `[[0]*M for _ in range(N)]`.
- **좌표 확장(핵심 잡기술)**: 연속 좌표계(선·도형)를 정수 격자로 옮길 때, 두 선
  사이 간격이 1이면 격자에서 **옆칸으로 붙어버려 존재하지 않는 지름길**이 생긴다.
  → **모든 좌표를 2배로 늘리고 마지막에 답을 `// 2`**. 간격 1이 2가 되어 사이에
  반드시 벽 칸이 끼어든다. "선을 칸으로 표현할 때"의 표준 대응.
- 영역 칠하기: "전부 채우기 → 전부 내부 지우기"를 **두 개의 분리된 루프**로.
  도형마다 채우고-지우고를 번갈아 하면 뒤 도형이 앞 도형의 테두리를 덮어써서 틀림.
- 함정: `[[0]*M]*N` 얕은 복사 / 행·열을 x·y로 헷갈림(r,c로 통일) /
  **격자 해상도가 원본 좌표와 같아도 되는지 항상 의심할 것**
- 내가 틀린 이력: 아이템 줍기(87694) — 로직은 맞았고 **해상도(좌표 2배)를 빠뜨림**,
  폭 1짜리 틈을 BFS가 1칸에 관통 → 정확성 70%
- **열/행 단위로 순서대로 쌓고 꺼내는 유형**(인형 뽑기류): 각 열을 `deque`로 만들어
  위에서부터 순서대로 append, `popleft()`로 꺼냄. 결과를 담는 별도 스택(리스트)에서
  "연속된 두 항목이 같은지"는 매번 전체를 훑지 말고 `stack[-1]`(top)만 비교.
- 관련 문제: 크레인 인형뽑기 게임(64061)

## 문자열 파싱
- 신호: "정해진 형식의 문자열이 주어진다", "괄호/쉼표로 구분된 중첩 구조",
  "공백/구분자로 나뉜 단어별로 각각 처리", "원본의 구분자 구조를 그대로 유지해야 한다"
- 골격: 중첩 괄호는 스택으로 깊이를 직접 추적하기보다, `re.findall(r'\{[^{}]*\}', s)`로
  "가장 안쪽 덩어리"만 통째로 뽑는 게 실수가 적다. 이후 `set(map(int, t.strip('{}').split(',')))`
  로 토큰마다 세트화.
  **원본의 공백/구분자 구조(연속 공백, 앞뒤 공백)를 그대로 보존해야 하는 문제**는
  `split()`/`join()`을 쓰면 그 정보가 사라짐 — 문자열을 1회 순회하며 인덱스를 직접
  관리하고, 구분자를 만나면 인덱스만 리셋한 뒤 구분자 자체는 그대로 결과에 추가.
- 함정: 크기(원소 개수)순 정렬과 등장 순서를 헷갈리지 말 것 — "몇 번째로 나왔나"가
  아니라 "집합 크기가 몇인가"로 정렬해야 함. 정렬 후 인접 항목의 **차집합**이
  새로 추가된 원소.
- 관련 문제: 튜플(64065), 이상한 문자 만들기(12930)

## 해시
- 신호: "짝을 찾아라", "몇 번 나왔나", "중복 제거", "존재 여부 빠르게 확인"
- 골격: Counter로 카운트 후 연산. **차이를 구할 때** `Counter(a) - Counter(b)`는
  음수를 자동으로 버려서 "a에만 있는 것"을 딱 뽑아냄. 파이썬답게는 `.items()`로
  루프 쓰기보다, 남는 게 1개 이하로 정해지면 `next(iter(result))`로 한줄.
- 함정: dict를 `for k, v in dict`로 돌리려 하면 key만 나옴 → `.items()` 필수.
  Counter 뺄셈이 음수를 버린다는 걸 모르고 다른 방식으로 우회하기 쉬움.
- **경우의 수 곱셈**: "종류별로 하나씩 고르거나 안 고를 수 있다" → 종류별 개수를
  세서 `(개수+1)`을 전부 곱하고, 마지막에 "아무것도 안 고른 경우" 1을 뺀다
  (`reduce(mul, (c+1 for c in Counter(...).values())) - 1`).
- 관련 문제: 완주하지 못한 선수(42576), 의상(42578)

## 정렬
- 신호: "우선순위 높은 것부터", "여러 기준으로 정렬" → 정렬 문제.
  "숫자들을 이어붙여 가장 크게/작게 만들어라"도 정렬 문제(비교 기준이 특수할 뿐).
- 골격: `sorted(dict.items(), key=lambda x: (-x[1], x[0]))` 패턴으로 다중 기준 정렬
  (1순위 내림차순, 같으면 2순위 오름차순). 정렬 후 리스트 컴프리헨션으로
  필요한 값만 추출.
  **자릿수가 다른 값끼리 비교**해야 하면 일반 크기비교(`a > b`)가 아니라
  `functools.cmp_to_key`로 "이어붙인 결과"(`a+b` vs `b+a`)를 비교. 모두 0인
  경우("00…0" → "0") 별도 처리 잊지 말 것.
- 함정: 누적으로 분모를 감소시키는 로직에서 0-나눗셈 주의 (`if n == 0` 체크 필수).
  sorted()의 key 함수에서 튜플을 돌려주면 앞 원소부터 차례대로 비교됨.
- 관련 문제: 실패율(42889), 가장 큰 수(42746)

## 스택·큐
- 신호: "가장 최근 것부터", "먼저 들어온 것부터", "괄호 짝", "대기열 처리", 
  "다음의 더 큰/작은 값", "연속된 중복 제거"
- 골격: 스택 = 여는 괄호는 쌓고 닫는 괄호는 맨 위와 짝 확인 (LIFO).
  큐 = deque(enumerate(...))로 (인덱스, 값) 쌍 유지, popleft()로 앞에서 꺼내고
  append()로 뒤에 보냄. "뒤에 더 높은 우선순위가 있는가"는 매번 any()로 O(N) 체크.
  **연속 원소 판정(중복 제거 등)**은 방향(앞/뒤) 상관없이 "직전에 채택한 값"과만
  비교하면 됨 — 표준형은 앞에서부터 순회 + `if not result or result[-1] != x:`.
- 함정: 큐를 list로 만들고 pop(0) 사용하면 O(N) → 시간초과. **deque 필수**.
  스택/큐가 비었는지 항상 먼저 확인. **while 루프 안에서 반복적으로 pop할 때는
  "비었는지" 체크를 매 반복마다** 해야 함 — 바깥에서 1번 검사한 것만 믿으면
  루프 도중 빈 상태가 될 수 있어 IndexError.
- 관련 문제: 프로세스(42587), 같은 숫자는 싫어(12906)

## 힙
- 신호: "가장 작은/큰 값을 반복해서 꺼내야 한다", "매번 최소/최대를 유지해야 한다".
  정렬은 1회성이지만 힙은 원소가 계속 추가/삭제되는 상황에서 매번 최소/최대를
  O(log N)에 유지.
- 골격: `heapq.heapify(arr)`로 O(N) 변환 후 `heappop`/`heappush`로 O(log N) 유지.
  최댓값이 필요하면 부호를 뒤집어 저장(`-val`) — Python heapq는 최소힙만 지원.
- 함정: **`heapq.heapify()`는 in-place라 반환값이 `None`** — `x = heapq.heapify(arr)`로
  재할당하면 x는 None. `heapify(arr)`만 호출하고 arr을 그대로 계속 사용할 것.
  정렬 유지하며 매번 리스트에 삽입(`bisect.insort`)하면 삽입 자체가 O(N)이라
  전체 O(N²)로 느려짐 — 반복적 삽입/삭제엔 힙이 정답.
- 관련 문제: 더 맵게(42626)

## BFS·DFS
- 신호: "최단 거리/최소 이동 횟수" → BFS(큐). "모든 경로/조합 탐색", "연결된 덩어리
  개수" → DFS 또는 BFS 아무거나.
- 골격:
  ```python
  from collections import deque
  q = deque([(sr, sc, 0)])            # (좌표..., 거리)를 튜플로 함께 실어 보냄
  visited = {(sr, sc)}                # set 리터럴. 넣는 시점은 "큐에 append할 때"
  while q:
      r, c, dist = q.popleft()
      if (r, c) == target: return dist    # pop 시점 검사 → 최단성 보장
      for k in range(4):
          nr, nc = r + dr[k], c + dc[k]
          if 범위 and 통행가능 and (nr, nc) not in visited:
              visited.add((nr, nc)); q.append((nr, nc, dist + 1))
  return -1                            # 도달 불가 시. 빠뜨리면 None 반환
  ```
- 함정: `visited` 표시를 **pop할 때** 하면 같은 칸이 큐에 여러 번 들어가 폭발 →
  **append할 때** 표시. / `deque` 대신 list+`pop(0)`은 O(N) → 시간초과. /
  `set([(x,y)])`를 `set((x,y))`로 쓰면 원소 2개짜리 집합이 됨(튜플이 풀림). /
  set에는 `.append()`가 없고 `.add()`. / while 종료 후 return 누락. /
  이동 가능 조건은 **범위 + 벽 + visited** 세 개를 항상 세트로 — 방향벡터
  개수(`len(dx)`)와 `for i in range(k)`의 k가 일치하는지 짜자마자 대조할 것. /
  좌표를 큐·visited·비교식에서 **tuple/list 중 하나로 통일** — `neighbor`는 list인데
  `visited`나 목표점 비교는 tuple이면 `==`/`in`이 항상 False로 조용히 깨짐. 특히
  함수를 합치는 리팩토링 직후 이 통일성이 제일 쉽게 무너지니 재확인.
- 내가 틀린 이력: 아이템 줍기(87694) — 자세한 건 2D 격자 항목 참고 /
  게임 맵 최단거리(1844) — range(4) 대신 range(3), 벽 체크 누락(힌트 1단계로 발견) +
  리팩토링 중 list/tuple 혼용으로 도착 판정이 항상 False
- DFS(카운트형) 골격: 각 갈래(선택지)마다 재귀 호출하고, leaf에서
  `1 if 조건 else 0`을 반환, 부모는 자식 반환값을 **그냥 더해서** 올려보낸다.
  ```python
  def dfs(i, state):
      if i == n:
          return 1 if 조건 else 0
      return dfs(i+1, 다음상태1) + dfs(i+1, 다음상태2)
  ```
  nonlocal 리스트에 값을 쌓고 나중에 세는 방식보다 이쪽이 안전 — non-leaf가
  명시적 `return`을 빠뜨리면 `None`이 섞여도 우연히 카운트가 맞는 경우가 있어
  버그를 못 알아채기 쉬움(타겟 넘버(43165)에서 확인, 결과적으론 정답이었음).
- 컴포넌트 개수 세기(상황 B)는 "미방문 집합"을 별도로 만들어 remove/next(iter)로
  관리할 필요 없음 — `visited` set 하나만 두고 `for i in range(n)`으로 전체
  인덱스를 순회하며 "아직 안 본 것"만 새 BFS/DFS 시작점으로 삼는 게 표준형
  (네트워크(43162)에서 확인).

## 완전탐색·백트래킹
- 신호: "가능한 모든 경우를 다 확인해라", "고정된 패턴을 반복해서 채운다",
  "숫자 조각/카드로 만들 수 있는 모든 조합/순서"
- 골격: 패턴이 짧고 반복된다면 `pattern * N`으로 answers보다 길게 늘려버리고
  `zip(pattern, answers)`으로 바로 비교 — 인덱스 나머지 연산(`i % len`)보다 단순.
  **순열로 만들 수 있는 모든 수**를 구할 때는
  `{int(''.join(p)) for i in range(1, len(s)+1) for p in permutations(s, i)}`
  — set comprehension 하나로 "길이가 다른 순열이 선행 0 때문에 같은 값이 되는 경우"
  까지 한 번에 중복 제거된다 (소수 찾기류).
- 함정: **동점 처리 누락 금지** — 최고 점수가 여러 명일 수 있으니 `max()`로 최고점을
  구한 뒤 `enumerate`로 그 점수와 같은 사람을 전부(리스트로) 모아야 함.
  완전탐색으로 만든 후보를 **판정하는 함수(소수 판정 등)의 복잡도를 따로 안 챙기면**
  후보 개수는 작아도 판정 자체가 병목이 될 수 있음 — 소수 판정은 반드시
  `range(2, int(x**0.5)+1)`로 √x bound.
- 내가 틀린 이력: 소수 찾기(42839) — #복잡도역산누락, 소수 판정을 `range(2, x)`로
  짜서 자릿수가 늘면 시간초과 위험(통과는 했으나 잠재 버그)
- 관련 문제: 모의고사(42840), 소수 찾기(42839)

## 그리디
- 신호: "최소/최대를 구하라"인데 완전탐색은 너무 크고, "각 단계에서 가장 좋아 보이는
  선택을 하면 전체적으로도 최적"이라는 게 성립할 때. "그럴듯해 보인다"만으로 접근하면
  위험 — 반례가 있는지 늘 의심.
- 골격: **묶어서 최솟값/최댓값이 대표되는 그룹 문제**는 정렬 후 연속 슬라이싱이
  "비슷한 값끼리 묶기"가 된다. 나머지(딱 안 나눠떨어지는 개수)는 정렬 후 **가장 낮은
  쪽**을 잘라 버려야 남는 그룹들의 대표값 합이 최대가 된다.
  ```python
  to_cut = len(arr) % m
  grouped = sorted(arr)[to_cut:]                      # 낮은 값부터 버림
  answer = sum(grouped[i] * m for i in range(0, len(grouped), m))  # 그룹 최솟값 × m
  ```
- 함정: 어떤 기준(오름차순/내림차순, 어느 쪽을 버릴지)이 손해가 없는지 스스로
  말로 증명 안 하고 "일단 정렬"만 하면 나머지 처리에서 헷갈리기 쉬움.
- 내가 틀린 이력: 과일 장수(135808) — #유형인식실패, "묶음의 가격은 최솟값이
  좌우한다 → 비슷한 값끼리 모아야 손해가 없다"는 연결을 바로 못 하고 힌트 1단계 필요.
- 관련 문제: 과일 장수(135808)

## 이분탐색
- 신호: "정렬된 배열에서 값을 빠르게 찾아라" + **"조건을 만족하는 최소/최대 값을
  찾아라"(파라메트릭 서치)**도 이분탐색. 후자가 시험 빈도 더 높음.
- 골격: 표준형은 `bisect.bisect_left/right`로 직접 루프 없이 한 줄.
  "정렬 후 누적합이 한도 이하인 최대 개수" 패턴은
  `bisect.bisect_right(list(accumulate(sorted(arr))), 한도)`.
  파라메트릭형은 "가능한가?" 판정 함수 + `lo/hi` 좁히기.
- 함정: `bisect`는 입력이 정렬돼 있다고 **그냥 믿고** 도는 함수라 정렬 안 하면
  에러 없이 조용히 틀린 값을 줌 — 항상 정렬 먼저. `bisect_left` vs `bisect_right`는
  "경계값을 포함하는지"에 따라 갈림(예산처럼 한도를 정확히 다 써도 되면 right).
- 관련 문제: 예산(12982)

## DP
(아직 없음)
