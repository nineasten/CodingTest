# [42576] 완주하지 못한 선수

- 인사이트: participant가 completion보다 정확히 1명 많다 → `Counter` 차이의
  총합은 항상 정확히 1 → 남는 항목은 항상 1개, 값은 항상 1. 조건문(`if v==1`)
  없이 `next(iter(Counter(a) - Counter(b)))`로 바로 꺼내면 됨.
- 힌트 1단계(관찰)까지 쓰고 스스로 완성. 문법 실수(Counter를 `for k,v`로 바로
  언패킹 → `.items()` 필요)는 있었지만 로직 자체는 처음부터 맞았음.
- 결과: 정답성 문제 없음, 제출 대기
