# [86051] 없는 숫자 더하기

- 인사이트: 고정 범위(0~N)에서 **정확히 1개**만 빠진 원소를 찾을 때
  → `전체합 - 부분합` (`45 - sum(numbers)`)가 가장 가볍다.
  빠진 게 여러 개거나 범위가 정확히 안 맞으면 `set(range(n)) - set(arr)` 차집합으로.
- 힌트 2단계(접근)까지 쓰고 스스로 완성
- 결과: ✅ 통과
